import { Tooltip as Tooltip$1 } from '@shopify/checkout-ui-extensions';
import { createRemoteReactComponent } from '@remote-ui/react';

const Tooltip = createRemoteReactComponent(Tooltip$1);

export { Tooltip };
