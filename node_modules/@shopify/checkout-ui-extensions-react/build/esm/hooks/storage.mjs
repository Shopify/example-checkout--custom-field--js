import { useExtensionApi } from './api.mjs';

/**
 * Returns the key-value `Storage` interface for the extension point.
 */
function useStorage() {
  return useExtensionApi().storage;
}

export { useStorage };
