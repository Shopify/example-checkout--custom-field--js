import { useExtensionApi } from './api.esnext';

/**
 * Returns the metadata about the extension.
 */
function useExtensionData() {
  return useExtensionApi().extension;
}

export { useExtensionData };
