import sqlite3 from 'sqlite3';
import { RdbmsConnection } from '@shopify/shopify-app-session-storage';
export declare class SqliteConnection implements RdbmsConnection {
    sessionStorageIdentifier: string;
    private ready;
    private db;
    constructor(database: string | sqlite3.Database, sessionStorageIdentifier: string);
    query(query: string, params?: any[]): Promise<any[]>;
    executeRawQuery(query: string): Promise<void>;
    hasTable(tablename: string): Promise<boolean>;
    getArgumentPlaceholder(_?: number): string;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    private init;
}
//# sourceMappingURL=sqlite-connection.d.ts.map